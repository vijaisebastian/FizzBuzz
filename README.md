● Create a console application that iterates through a range of numbers from 1 to 100.
● For numbers divisible by 3, output "Fizz"; for numbers divisible by 5, output "Buzz"; and
for numbers divisible by both, output "Fizzbuzz".
● For all other numbers, simply output the number itself.
● Display the results in a console window.
